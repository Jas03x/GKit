#ifndef GK_CLOCK_H
#define GK_CLOCK_H

/*
#include <SDL2/SDL.h>

class Clock
{
protected:
    uint32_t m_Previous;
    uint32_t m_Current;

public:
    Clock();

    void Update();
    uint32_t Delta();
};

class Timer : protected Clock
{
protected:
    uint32_t m_Period;

public:
    Timer(uint32_t period = 0);

    bool Tick();
    float Delta();
};
*/

#endif // GK_CLOCK_H